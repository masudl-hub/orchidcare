// Dashboard.tsx — backward compatibility re-export
// The dashboard has been refactored into:
//   DashboardShell.tsx  — layout, grain, navigation, tab routing
//   CollectionView.tsx  — plant collection grid
//   ActivityView.tsx    — activity timeline
//   ProfileView.tsx     — profile, protocols, insights, security

export { default } from './DashboardShell';
